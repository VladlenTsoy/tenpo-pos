# Reminder for Vladlen

When ready, send the dedicated Git repository for Tenpo POS so this local project can be pushed/migrated there.

Current local locations:
- Product/architecture docs: `/opt/ai-org/workspaces/core-director/projects/tenpo-pos`
- First backend implementation slice: `/opt/ai-org/workspaces/tenpo-dev/kokoro-api` on `develop`

Do not forget: create/provide a separate repo if POS should live outside existing Kokoro repos.
